<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="\Assets\images\logo.png" alt="Logo" style="height:40px;">Matthew Hanlan
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">

          
          <li class="nav-item">
            <a class= 
            <% if (title == 'Home') { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
            <% } %>
            aria-current="page" href="/home">
            <i class="fas fa-landmark"></i> Home </a>
          </li>

          <li class="nav-item">
            <a class= 
            <% if (title == 'About') { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
            <% } %>
            aria-current="page" href="/about">
              <i class="fas fa-info-circle"></i> About </a>
          </li>

          <li class="nav-item">
            <a class= 
            <% if (title == 'Projects') { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
            <% } %>
            aria-current="page" href="/projects">
            <i class="fas fa-pencil-ruler"></i> Projects </a>
          </li>

          <li class="nav-item">
            <a class= 
            <% if (title == 'Services') { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
            <% } %>
            aria-current="page" href="/services">
              <i class="fas fa-concierge-bell"></i> Services </a>
          </li>

          <li class="nav-item">
            <a class= 
            <% if (title == 'Contact') { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
            <% } %>
            aria-current="page" href="/contact">
            <i class="fas fa-phone-alt"></i> Contact </a>
          </li>

          <li class="nav-item">
            <a class= 
            <% if (title == 'Book List') { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
            <% } %>
            aria-current="page" href="/book-list">
            <i class="fas fa-book"></i> Contact List </a>
          </li>

          <% if(typeof displayname == 'string' && displayname) { %>
            <li class="navbar-text"><i class="fas fa-lg fa-user"></i>Welcome, <%= displayname %></li>
            <li class="nav-item"><a href="/logout" class="nav-link">
              <i class="fas fa-lg fa-sign-out-alt"></i>Logout</a></li>
          <% } else { %>
            <li class="nav-item"><a href="/login" class="nav-link">
            <i class="fas fa-lg fa-sign-in-alt"></i>Login</a></li>
          <% } %>
 

        </ul>
      </div>
    </div>
  </nav>